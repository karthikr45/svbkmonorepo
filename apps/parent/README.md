# @svbk/parent

Drop your existing parent portal project's source files into this folder.

The folder should contain its own `package.json` with:
- `"name": "@svbk/parent"`
- `"private": true`
- `dev`, `build`, `lint`, `test`, `typecheck` scripts

After copying files, run `pnpm install` from the repo root.
