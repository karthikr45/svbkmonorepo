# API base URL for all backend requests (used by lib/api-client).
# NestJS uses globalPrefix 'api' — include it in the URL.
# Local dev: api runs on port 3001
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api
